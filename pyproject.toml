[build-system]
requires = ["setuptools>=61", "setuptools-scm>=8.0"]


[project]
name = "PyEarthTools-archive-JASMIN"
description = "PyEarthTools archive for JASMIN"
requires-python = ">=3.9"
license = {file = "LICENSE"}
keywords = ["pyearthtools"]
maintainers = [
  {email = "stephen.haddad@metoffice.gov.uk", name = "Stephen Haddad"}
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
]
dependencies = [
  "pyearthtools-data>=0.1.0",
]
dynamic = ["version", "readme"]

[tool.setuptools.dynamic]
readme = {file = ["README.md"], content-type = "text/markdown"}

[project.urls]
homepage = "https://pyearthtools.readthedocs.io/en/latest/"
documentation = "https://pyearthtools.readthedocs.io/en/latest/"
repository = "https://github.com/MetOffice/pyearthtools_jasmin"

[tool.isort]
profile = "black"

[tool.black]
line-length = 120

[tool.mypy]
warn_return_any = true
warn_unused_configs = true

[tool.hatch.version]
path = "src/site_archive_jasmin/__init__.py"
